.dfx/
.basilisk/
node_modules/
# frontend/dist is committed — it's the pre-built canister artifact
frontend/.svelte-kit/
__pycache__/
*.pyc
.env
# canister_ids/ is intentionally committed (mainnet canister ID mappings)
